{
  "name": "rensights-frontend-root",
  "private": true,
  "description": "Run npm scripts from src/ (Next.js app; Docker build context).",
  "scripts": {
    "dev": "npm run dev --prefix src",
    "build": "npm run build --prefix src",
    "start": "npm run start --prefix src",
    "lint": "npm run lint --prefix src",
    "clean": "npm run clean --prefix src && rm -rf .next node_modules/.cache"
  }
}
